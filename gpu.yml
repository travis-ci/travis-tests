jobs:
  include:
    - stage: "GPU - ADDONS"
      name: "ADDONS"
      vm:
        size: 'gpu-medium'
      arch: 'amd64'
      addons:
        tensorflow: 1.6.0
        blender: 3.4.1
      script:
        - echo "It is done"
    - stage: "GPU - windows - should not allow that"
      language: bash
      name: "windows"
      vm:
        size: 'gpu-medium'
      os: 'windows'
      script:
        - echo "It is done"
